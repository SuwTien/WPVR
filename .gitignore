# Config utilisateur locale (chemins de dossiers propres a chaque machine/utilisateur)
PhotoViewer.config.json

# Bloc-notes de logs temporaires colles depuis le terminal
LogsTmp.txt

# Classeurs generes par Export-ContactSheet.ps1 (sorties, pas du code source)
PlancheContact_*.xlsx
