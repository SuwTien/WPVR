<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Photo Viewer" Width="480" Height="800" MinWidth="320" MinHeight="400"
        Background="#FF1E1E1E">
    <Window.Resources>
        <!-- Bouton sans chrome par defaut : evite le halo gris au survol souris des boutons
             transparents du plein ecran (fleches, fermeture). -->
        <Style x:Key="TransparentIconButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="8,8,8,4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock x:Name="DirectoryLabel" Grid.Column="0" VerticalAlignment="Center"
                       FontSize="13" Foreground="#FFCCCCCC"
                       TextTrimming="CharacterEllipsis"/>

            <!-- Badge indiquant si le repertoire actif autorise le renommage/suppression -->
            <Border Grid.Column="1" CornerRadius="4" Padding="8,3" Margin="0,0,8,0" VerticalAlignment="Center">
                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="Background" Value="#FF3C7A3C"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsReadOnly}" Value="True">
                                <Setter Property="Background" Value="#FFB5484C"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <TextBlock Foreground="White" FontSize="11" FontWeight="Bold">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Text" Value="MODIFIABLE"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsReadOnly}" Value="True">
                                    <Setter Property="Text" Value="LECTURE SEULE"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </Border>

            <Button x:Name="ChooseDirectoryButton" Grid.Column="2" Padding="10,6"
                    Content="Dossier..." Command="{Binding ChooseDirectoryCommand}"/>
        </Grid>

        <!-- Barre d'outils : actions frequentes, sur sa propre ligne pour rester lisible en largeur reduite -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="8,0,8,8">
            <Button x:Name="CameraButton" Padding="10,6" Margin="0,0,8,0" Content="PHOTO" FontWeight="Bold"/>
            <Button x:Name="RefreshButton" Padding="10,6" Margin="0,0,8,0" Content="Rafraichir"/>
            <Button x:Name="ToggleDirectoryButton" Padding="10,6" Margin="0,0,8,0"
                    Content="Basculer" Command="{Binding ToggleDirectoryCommand}"/>
            <Button x:Name="ExportContactSheetButton" Padding="10,6" Content="Export planche contact"/>
        </StackPanel>

        <ScrollViewer x:Name="GridScrollViewer" Grid.Row="2"
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled">
            <ItemsControl x:Name="RowsItemsControl">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <!-- Virtualisation native : seules les lignes visibles sont réalisées -->
                        <VirtualizingStackPanel Orientation="Vertical"
                                                 VirtualizingPanel.IsVirtualizing="True"
                                                 VirtualizingPanel.VirtualizationMode="Recycling"
                                                 VirtualizingPanel.ScrollUnit="Pixel"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <!-- Chaque item de RowsItemsControl est lui-même une collection de photos (une ligne) -->
                        <ItemsControl ItemsSource="{Binding}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Horizontal"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Button Width="150" Height="150" Margin="4" Padding="0"
                                            BorderThickness="0" Background="#FF2D2D30"
                                            HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch"
                                            Command="{Binding DataContext.TapCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                            CommandParameter="{Binding}">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="*"/>
                                                <RowDefinition Height="Auto"/>
                                            </Grid.RowDefinitions>
                                            <Image Grid.Row="0" Source="{Binding Thumbnail}"
                                                   Stretch="Uniform"/>
                                            <TextBlock Grid.Row="1" Text="{Binding FileName}"
                                                       Foreground="White" FontSize="10"
                                                       TextTrimming="CharacterEllipsis"
                                                       Margin="4,2" HorizontalAlignment="Center"/>
                                        </Grid>
                                    </Button>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- Mode plein ecran (etape 5) : recouvre toute la fenetre, cache par defaut -->
        <Grid x:Name="FullScreenOverlay" Grid.Row="0" Grid.RowSpan="3"
              Background="#FF000000" Visibility="Collapsed" Focusable="True">
            <Border x:Name="FullScreenImageContainer" ClipToBounds="True">
                <Image x:Name="FullScreenImage" Stretch="Uniform" RenderTransformOrigin="0.5,0.5">
                    <Image.RenderTransform>
                        <TransformGroup>
                            <ScaleTransform x:Name="FullScreenScale"/>
                            <TranslateTransform x:Name="FullScreenTranslate"/>
                        </TransformGroup>
                    </Image.RenderTransform>
                </Image>
            </Border>

            <!-- Fleches larges et hautes (3/4 de la hauteur) pour un ciblage tactile facile -->
            <Grid HorizontalAlignment="Left" Width="90" Margin="8,0,0,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="1*"/>
                    <RowDefinition Height="6*"/>
                    <RowDefinition Height="1*"/>
                </Grid.RowDefinitions>
                <Button x:Name="FullScreenPrevButton" Grid.Row="1" Content="&#8249;" FontSize="40"
                        Style="{StaticResource TransparentIconButtonStyle}" Foreground="White"/>
            </Grid>
            <Grid HorizontalAlignment="Right" Width="90" Margin="0,0,8,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="1*"/>
                    <RowDefinition Height="6*"/>
                    <RowDefinition Height="1*"/>
                </Grid.RowDefinitions>
                <Button x:Name="FullScreenNextButton" Grid.Row="1" Content="&#8250;" FontSize="40"
                        Style="{StaticResource TransparentIconButtonStyle}" Foreground="White"/>
            </Grid>

            <Button x:Name="FullScreenCloseButton" Content="&#10005;" FontSize="18" Width="44" Height="44"
                    HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,8,8,0"
                    Style="{StaticResource TransparentIconButtonStyle}" Foreground="White"/>

            <Button x:Name="FullScreenFileNameButton" VerticalAlignment="Bottom" HorizontalAlignment="Stretch"
                    Height="48" Background="#AA000000" BorderThickness="0" Foreground="White" FontSize="14"
                    HorizontalContentAlignment="Center" VerticalContentAlignment="Center"/>
        </Grid>
    </Grid>
</Window>
